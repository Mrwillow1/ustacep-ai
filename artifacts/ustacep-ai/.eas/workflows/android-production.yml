name: Android Production AAB

on:
  push:
    branches:
      - main

jobs:
  build_android:
    name: Build Android production AAB
    type: build
    params:
      platform: android
      profile: production